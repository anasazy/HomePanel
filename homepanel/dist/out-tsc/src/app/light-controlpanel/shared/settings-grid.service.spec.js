import { TestBed } from '@angular/core/testing';
import { SettingsGridService } from './settings-grid.service';
describe('SettingsGridService', () => {
    beforeEach(() => TestBed.configureTestingModule({}));
    it('should be created', () => {
        const service = TestBed.get(SettingsGridService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=settings-grid.service.spec.js.map