import { TestBed } from '@angular/core/testing';
import { SwitchGridService } from './switch-grid.service';
describe('SwitchGridService', () => {
    beforeEach(() => TestBed.configureTestingModule({}));
    it('should be created', () => {
        const service = TestBed.get(SwitchGridService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=switch-grid.service.spec.js.map